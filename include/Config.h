#pragma once

struct Config {
    unsigned windowWidth    = 800;
    unsigned windowHeight   = 600;
    unsigned targetFps      = 60;
    int gravity             = -10;
    int scrollSpeed         = 100;
};
